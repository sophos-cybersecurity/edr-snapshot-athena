/* 
Use this script to create a database, by default it will be created in the default AWS Glue data catalog, 
if you want something different you probably already know how to do this! 

This database name will be referenced in all the SQL statements in this repo, so make sure to change them if you do something different here 
*/

CREATE DATABASE interceptx_edr;
